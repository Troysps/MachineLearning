week 1
线性回归实现
	--假设函数 hx = theta0 + theta1*x1 + ... + thetai*xi
	--代价函数 J(theta) = (hx - y)**2
	--梯度下降法求 theta  批量更新
		-- theta0 = theta0 - alpha * f(theta') * J(theta)
		-- theta1 = theta1 - alpha * f(theta') * J(theta) * x1
		
		即为 thetai = thetai - alpha * f(theta') * J(theta) * xi
		

		
week 2 矩阵和向量
	--矩阵 
	--维度 number of rows and number of columns
	4x2 的矩阵 : R^4x2
	
	matrix elements 
	A_ij = i, j entry in the i^th rows j^th columns
	
	
	
	--向量
		只有一列
		向量只有一列 n行
		--0索引 1索引
		
	
	矩阵计算
	1. 相加 --逐个元素相加
	2. 矩阵和标量(运算： 都和标量相乘
	3. 矩阵与向量相乘
	4. 矩阵与矩阵相乘
	
	
	矩阵相乘特性
	--AxB ！= BxA
	AxBxC
	矩阵不服从交换率
	
	单位矩阵服从交换率:
	identity matrix ：A I = I A = A
	
	

	矩阵的逆矩阵与转置计算
	
	实数的倒数
	
	--矩阵的逆矩阵与转置计算
	A * A^-1 = A^-1 * A = I
	如果a是一个矩阵 并且有其逆矩阵
	
	逆矩阵计算
	计算精度 四舍五入产生误差
	
	可逆矩阵必须是方阵矩阵
	没有逆矩阵的矩阵称为奇异矩阵
	
	
	
	矩阵的转置
	A B = A^T
	B_ij = A_ji
	
	

		
		